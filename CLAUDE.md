# Claude development context

Use the same repository contracts as `AGENTS.md`. The canonical implementation artifacts are the v2 design and v2 implementation plan dated 2026-09-09. Treat v1 artifacts as historical architecture evidence only.

For investigative wording, distinguish reconstruction from search planning. Reconstruction must not seed unsupported concrete locations; search planning may propose concrete locations only as explicit proposals. Generic hosts do not provide a mandatory repository-controlled pre-send interception hook, so never overclaim guard coverage.
