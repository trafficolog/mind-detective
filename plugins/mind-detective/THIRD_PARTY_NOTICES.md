# Third-Party Notices

The `mind-detective` 0.1.0 runtime uses the Python standard library and bundles no third-party runtime SDK or library.

Scientific and methodological papers cited in `docs/METHODOLOGY*` are references only; no paper text, code, dataset, or licensed asset is bundled into the plugin.
